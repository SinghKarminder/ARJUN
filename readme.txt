. a seprate python file will handle the wget

. wget arjun.config
	$ config file once read will be get a param "read = true"
. 	$ file consist of required thresholding paramters as
	  required by the cam file selected based on file output and
	custom test
	
. wget new files if set in config file

. 
